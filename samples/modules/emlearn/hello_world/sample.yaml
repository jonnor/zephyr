sample:
  description: emlearn sample
  name: hello world emlearn
common:
  tags: emlearn
  modules:
    - emlearn
  harness: console
  harness_config:
    type: multi_line
    ordered: false
    regex:
      - "x_value:.*, y_value:.*"
      - ""
tests:
  sample.emlearn.helloworld:
    platform_allow: qemu_x86 qemu_x86_64
    integration_platforms:
      - qemu_x86
    tags: emlearn
